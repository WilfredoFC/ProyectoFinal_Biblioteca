<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <!-- 1. Estilo para ContentPage (Implícito) -->
    <Style TargetType="ContentPage" ApplyToDerivedTypes="True">
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource PageBackgroundColor}, Dark={StaticResource PageBackgroundColorDark}}"/>
    </Style>

    <!-- 2. Estilo para NavigationPage (Barra superior del Flyout) -->
    <Style TargetType="NavigationPage" ApplyToDerivedTypes="True">
        <Setter Property="BarBackgroundColor" Value="{StaticResource Primary}"/>
        <Setter Property="BarTextColor" Value="White"/>
    </Style>

    <!-- 3. Estilo para Título Principal -->
    <Style x:Key="HeaderLabelStyle" TargetType="Label">
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontAttributes" Value="Bold"/>
        <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource PrimaryTextColor}, Dark={StaticResource PrimaryTextColorDark}}"/>
        <Setter Property="Margin" Value="20,10,0,0"/>
    </Style>

    <!-- 4. Estilo para Subtítulo -->
    <Style x:Key="SubHeaderLabelStyle" TargetType="Label">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource SecondaryTextColor}, Dark={StaticResource SecondaryTextColorDark}}"/>
        <Setter Property="Margin" Value="20,0,0,10"/>
    </Style>

    <!-- 5. Estilo para Tarjetas (Frame) -->
    <Style x:Key="CardFrameStyle" TargetType="Frame">
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="15"/>
        <Setter Property="Margin" Value="10,5"/>
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light={StaticResource CardBackgroundColor}, Dark={StaticResource CardBackgroundColorDark}}"/>
        <Setter Property="BorderColor" Value="Transparent"/>
        <Setter Property="Shadow" Value="0,2,5,0,0,0,0.2"/>
    </Style>

    <!-- 6. Estilo para Campos de Texto (Entry) -->
    <Style x:Key="FormEntryStyle" TargetType="Entry">
        <Setter Property="BackgroundColor" Value="Transparent"/>
        <Setter Property="TextColor" Value="{AppThemeBinding Light={StaticResource PrimaryTextColor}, Dark={StaticResource PrimaryTextColorDark}}"/>
        <Setter Property="PlaceholderColor" Value="{AppThemeBinding Light={StaticResource SecondaryTextColor}, Dark={StaticResource SecondaryTextColorDark}}"/>
    </Style>

    <!-- 7. Estilo para Botones Principales -->
    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="BackgroundColor" Value="{StaticResource Primary}"/>
        <Setter Property="TextColor" Value="White"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="HeightRequest" Value="45"/>
        <Setter Property="FontAttributes" Value="Bold"/>
    </Style>

    <!-- 8. Estilo para CollectionView (Opcional, pero suma puntos) -->
    <Style TargetType="CollectionView">
        <Setter Property="BackgroundColor" Value="Transparent"/>
    </Style>
</ResourceDictionary>